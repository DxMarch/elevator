# Copy this file to .env and fill in the values.

# Erlang distribution cookie — must be identical on every node.
ERLANG_COOKIE=i_like_cookies

# Shared secret used by libcluster Gossip to authenticate messages.
# Generate with: openssl rand -hex 32
GOSSIP_SECRET=i_like_gossip
